# Server Frontend

Laptop-facing host screen.

This area will render the lobby, join code, connected players, race view, countdowns, player status, and results.

## MVP Behavior

- Creates a room code.
- Shows two player slots.
- Receives and displays live WebRTC camera streams from phones.
- Shows basic connection status.

Run with:

```sh
npm run dev
```

Then open `/host`.
